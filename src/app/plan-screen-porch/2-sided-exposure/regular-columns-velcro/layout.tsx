import type { Metadata } from 'next'

export const metadata: Metadata = ./2-sided-exposure/regular-columns-velcro/layout.tsx

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
