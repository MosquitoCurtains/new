import type { Metadata } from 'next'

export const metadata: Metadata = ./outdoor-curtain-tracking/layout.tsx

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
