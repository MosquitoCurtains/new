import type { Metadata } from 'next'

export const metadata: Metadata = ./3-sided-exposure/layout.tsx

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
