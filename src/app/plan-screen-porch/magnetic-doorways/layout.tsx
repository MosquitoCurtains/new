import type { Metadata } from 'next'

export const metadata: Metadata = ./magnetic-doorways/layout.tsx

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
