import type { Metadata } from 'next'

export const metadata: Metadata = ./mesh-and-colors/layout.tsx

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
