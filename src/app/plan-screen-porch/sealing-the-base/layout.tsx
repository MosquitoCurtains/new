import type { Metadata } from 'next'

export const metadata: Metadata = ./sealing-the-base/layout.tsx

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
