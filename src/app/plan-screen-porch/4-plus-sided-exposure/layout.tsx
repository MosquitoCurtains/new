import type { Metadata } from 'next'

export const metadata: Metadata = ./4-plus-sided-exposure/layout.tsx

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
